import styled from 'styled-components';
import { Color } from '../../styles/variables/colors';

export const CurrencyInputTitle = styled.div`
  font-size: 1rem;
  line-height: 1.5rem;
  font-weight: 500;
  color: ${Color.Secondary};
`;
